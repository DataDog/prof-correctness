{
  "test_name": "python_exceptions",
  "note": "500 identical ValueError raises. Observed share is 4% because exception-samples mix in profiler/startup frames; the sampler does not emit one sample per raise. absDiff cannot express a floor (50±40 failed CI at observed 4%, 46pp). Keep 4±4 until the harness grows a min assertion or the workload is rewritten to exclude startup.",
  "stacks": [
    {
      "profile-type": "exception-samples",
      "stack-content": [
        {
          "regular_expression": ".*raise_value_error.*",
          "percent": 4,
          "error_margin": 4,
          "labels": [
            {
              "key": "exception type",
              "values": [
                "builtins.ValueError"
              ]
            }
          ]
        }
      ]
    }
  ],
  "scale_by_duration": false
}
